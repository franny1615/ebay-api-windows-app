<Window x:Class="ebay_api_inventory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ebay_api_inventory" 
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="450" 
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Column="1" 
                   Grid.Row="0">
            <wv2:WebView2 x:Name="WebView"
                          Source="https:\\www.google.com"
                          VerticalAlignment="Stretch" 
                          NavigationCompleted="Navigation_Finished"/>
        </DockPanel>

        <Button x:Name="SettingsButton"
                Click="SettingsButton_Clicked"
                Background="Transparent"
                Width="50" 
                Height="25" 
                Grid.Row="2" 
                HorizontalAlignment="Left"
                BorderThickness="0"
                Margin="10,0,0,0">
            <Image Source="/Resources/gear.png" ToolTip="Settings" />
        </Button>
    </Grid>
</Window>
